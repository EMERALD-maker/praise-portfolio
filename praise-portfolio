# 1. Create React app
npx create-react-app praise-portfolio
cd praise-portfolio

# 2. Install Framer Motion & GitHub Pages
npm install framer-motion
npm install gh-pages --save-dev

# 3. Update package.json
#   Add this at the top level:
#   "homepage": "https://<your-github-username>.github.io/praise-portfolio"
#   Under "scripts", add:
#   "predeploy": "npm run build",
#   "deploy": "gh-pages -d build"

# 4. Initialize Git and push to GitHub
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/<your-github-username>/praise-portfolio.git
git push -u origin main

# 5. Deploy to GitHub Pages
npm run deploy
